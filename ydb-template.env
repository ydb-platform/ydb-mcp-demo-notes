# YDB Connection Configuration Template
# ====================================
# Copy this file to ydb.env and modify the settings for your environment
# Command: cp ydb-template.env ydb.env

# YDB Connection Parameters
# ========================

# For local YDB development (Docker container)
YDB_ENDPOINT=grpc://localhost:2136
YDB_DATABASE=/local

# For Yandex Cloud YDB (replace with your values)
# YDB_ENDPOINT=grpcs://ydb.serverless.yandexcloud.net:2135/?database=/ru-central1/YOUR_FOLDER_ID/YOUR_DATABASE_ID
# YDB_DATABASE=/ru-central1/YOUR_FOLDER_ID/YOUR_DATABASE_ID

# Authentication Configuration
# ===========================
# Choose ONE authentication method and uncomment the corresponding lines

# 1. Anonymous authentication (for local development)
YDB_ANONYMOUS_CREDENTIALS=1

# 2. Service Account Key File (for Yandex Cloud)
# AUTH_MODE=env
# YDB_SERVICE_ACCOUNT_KEY_FILE_CREDENTIALS=authorized_key.json
# YDB_AUTH_MODE=service-account
# YDB_SA_KEY_FILE=authorized_key.json

# 3. Access Token (for Yandex Cloud)
# AUTH_MODE=env
# YDB_ACCESS_TOKEN_CREDENTIALS=your-access-token-here
# YDB_AUTH_MODE=access-token

# 4. Metadata service (for Yandex Cloud Compute instances)
# AUTH_MODE=env
# YDB_METADATA_CREDENTIALS=1
# YDB_AUTH_MODE=metadata

# Notes Application Settings
# =========================
# AUTH_MODE determines how the notes.py application authenticates
# - anonymous: Use anonymous authentication (default for localhost)
# - env: Use authentication from environment variables above
AUTH_MODE=anonymous

# Usage Examples
# ==============
# 
# Local development:
# 1. Copy this file: cp ydb-template.env ydb.env
# 2. Use as-is for localhost connection
# 3. Start local YDB: docker compose up -d ydb
# 
# Yandex Cloud:
# 1. Copy this file: cp ydb-template.env ydb.env
# 2. Replace YDB_ENDPOINT and YDB_DATABASE with your cloud values
# 3. Uncomment and configure one of the authentication methods
# 4. For service account: place your key file as authorized_key.json
#
# No configuration file:
# The system automatically uses localhost defaults when ydb.env is missing
